# 锁定公共 npm registry —— 提交进仓库，所有人（含 CI）一致。
#
# 为什么需要它：pnpm-lock.yaml 是要提交的，而 pnpm 只在
# 「生效 registry 不等于默认值」时才往每个包的 resolution 里写完整 tarball 地址。
# 之前没有这个文件，于是 lockfile 跟随了本机 ~/.npmrc 里的私服地址，1185 个包的
# tarball 全被固化成内网域名 —— GitHub runner 访问不到，`pnpm install
# --frozen-lockfile` 直接 ERR_SOCKET_TIMEOUT。
#
# 钉住公共源（pnpm 的默认 registry）后，lockfile 里不再出现 tarball 字段，
# URL 由 registry 配置推导，lockfile 因此可移植、且不会被本机私服再次污染。
#
# 代价：本机从公共源下载比走内网镜像慢（实测 tarball SHA 相同、速度约慢 6 倍）。
# store 有缓存，属一次性成本。想换回镜像就改这一行，但须同时重新生成
# lockfile，否则又会把私服地址写回去。
registry=https://registry.npmjs.org/
