# CLAUDE.md

本项目的智能体约定统一维护在 [AGENTS.md](AGENTS.md)（同一份内容也供 Codex / Cursor
等读取），这里用导入引入，避免两处分叉。

最要紧的一条：**动手前先 `nvm use`**（读 `.nvmrc` → Node 24）。本机 nvm 没设
`default` alias，新开终端默认是 Node 14，`pnpm` 会直接报错。

@AGENTS.md
